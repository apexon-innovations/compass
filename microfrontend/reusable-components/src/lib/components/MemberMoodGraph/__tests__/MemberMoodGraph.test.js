import React from 'react'
import { render } from '@testing-library/react'
import MemberMoodGraph from '../MemberMoodGraph'

describe('MemberMoodGraph component', () => {
  it('MemberMoodGraph rendered correctly', () => {
    render(<MemberMoodGraph />)
  })
})
