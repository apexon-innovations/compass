/* eslint-disable */
import { IMG_DASHBOARD_SWITCH } from '../imgConst'

describe(`Billing Box component`, () => {
  it('Billing Box rendered correctly', () => {
    expect(IMG_DASHBOARD_SWITCH).toEqual(IMG_DASHBOARD_SWITCH)
  })
})
